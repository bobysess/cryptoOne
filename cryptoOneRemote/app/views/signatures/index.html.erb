<h1>Listing signatures</h1>

<table>
  <thead>
    <tr>
      <th>Signorityid</th>
      <th>Userid</th>
      <th>Signature</th>
      <th>Encryptedpublickey</th>
      <th>Signaturedate</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @signatures.each do |signature| %>
      <tr>
        <td><%= signature.signorityId %></td>
        <td><%= signature.userId %></td>
        <td><%= signature.signature %></td>
        <td><%= signature.encryptedPublicKey %></td>
        <td><%= signature.signatureDate %></td>
        <td><%= link_to 'Show', signature %></td>
        <td><%= link_to 'Edit', edit_signature_path(signature) %></td>
        <td><%= link_to 'Destroy', signature, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Signature', new_signature_path %>
